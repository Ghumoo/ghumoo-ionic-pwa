import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ghumoo-ionic-pwa-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
